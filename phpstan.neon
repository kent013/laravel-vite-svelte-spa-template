includes:
    - phpstan-baseline.neon
parameters:
    level: max
    paths:
        - app
        - config
        - database
        - routes
        - tests
    parallel:
        processTimeout: 300.0
    # relative path to this file(phpstan.neon)
    tmpDir: ./tmp/phpstan